# Универсальная программа распознавания чисел с экрана

## Описание
Кроссплатформенная программа для распознавания чисел с экрана в реальном времени с расширенными возможностями настройки и автоматической конфигурацией.

## Функции
- 🌐 **Универсальность**: Работает на Windows, macOS и Linux
- 🔍 **Автоматическое обнаружение**: Находит Tesseract OCR в системе
- 📱 Распознавание чисел с выбранной области экрана
- 💾 Автоматическое сохранение в JSON при каждом новом числе
- 🌈 **Цветные таблицы**: Красивый вывод данных с цветовым кодированием
- 📊 **Расширенная статистика**: Анализ данных с медианой, стандартным отклонением, топ-10
- 📈 **Интеллектуальный анализ**: Распределение по диапазонам, цветовая индикация
- 💾 **Экспорт данных**: Экспорт в CSV для анализа в Excel/LibreOffice
- 🎯 **Счетчики серий**: Трекинг подряд идущих результатов по пользовательским меткам
- ⏰ Сохранение временных меток для каждого числа
- 🔄 Загрузка существующих данных при запуске
- ⚙️ Гибкая система конфигурации
- 💾 Сохранение областей экрана для повторного использования
- 🎯 Улучшенный интерфейс выбора области с отображением координат
- 🖥️ Отображение системной информации
- 🔧 Настраиваемые параметры обработки изображений

## Установка

### 1. Установка Tesseract OCR

#### macOS:
```bash
brew install tesseract
```

#### Ubuntu/Debian:
```bash
sudo apt-get install tesseract-ocr
```

#### Windows:
Скачайте и установите с https://github.com/UB-Mannheim/tesseract/wiki

#### Автоматическая установка (любая ОС):
Программа автоматически найдет Tesseract OCR в системе при первом запуске

### 2. Установка Python зависимостей
```bash
pip install -r requirements.txt
```

## Использование

### 🚀 Быстрая установка (macOS):
```bash
./install.sh
```

### 📋 Основные команды:

**Запуск программы мониторинга:**
```bash
./run.sh
```

**Просмотр сохраненных данных:**
```bash
./run.sh --view
```

**Настройка программы:**
```bash
./run.sh --setup
```

**Режим отладки:**
```bash
./run.sh --debug
```

**Использование пользовательской конфигурации:**
```bash
./run.sh --config my_config.json
```

**Создание демонстрационных данных:**
```bash
python3 demo.py
```

### 🎯 Использование программы:

1. **Первый запуск**: Программа автоматически найдет Tesseract OCR в системе
2. **Выбор области**: Интерактивный выбор с отображением координат курсора
3. **Сохранение области**: Выбранная область автоматически сохраняется
4. **Мониторинг**: Программа начнет распознавание и сохранение данных
5. **Выход**: Нажмите Ctrl+C для завершения

### 🎯 Счетчики серий:

Программа автоматически отслеживает серии подряд идущих результатов по пользовательским меткам:

- **Метки**: Пороговые значения (3.0, 4.0, 5.0, 6.0, 7.0)
- **Макс.серия**: Рекорд - максимальное количество подряд НЕ превысивших метку
- **Текущ.серия**: Текущая серия подряд НЕ превысивших метку

**Принцип работы:**
- Если распознанное число ≤ метки → серия продолжается
- Если число > метки → серия сбрасывается в 0
- Максимальная серия сохраняется как рекорд

**Пример:**
```
Метка 5.0: распознали 4.2, 3.8, 4.9, 6.1, 2.3
Результат: Макс.серия = 3, Текущ.серия = 1
```

### ⚙️ Ручной запуск (если нужно):
```bash
source venv/bin/activate
python 123456789000.py [опции]
```

### 📊 Опции командной строки:
- `--view` - Просмотр сохраненных данных с цветными таблицами
- `--stats` - Расширенная статистика с анализом данных
- `--series` - Счетчики серий (метки и рекорды)
- `--config FILE` - Указать файл конфигурации
- `--setup` - Настройка программы
- `--debug` - Режим отладки
- `--export FILE` - Экспорт данных в CSV файл
- `--clear-stats` - Очистить файл статистики
- `--clear-series` - Сбросить счетчики серий
- `--help` - Показать справку

## 🌐 Поддерживаемые операционные системы

| ОС | Статус | Примечания |
|---|---|---|
| 🍎 **macOS** | ✅ Полностью поддерживается | Автоматическое обнаружение Tesseract |
| 🪟 **Windows** | ✅ Полностью поддерживается | Поддержка различных путей установки |
| 🐧 **Linux** | ✅ Полностью поддерживается | Ubuntu, Debian, CentOS, RHEL |

Программа автоматически определяет операционную систему и находит Tesseract OCR в стандартных местах установки.

## Файлы

### 📁 Основные файлы:
- `123456789000.py` - основная программа
- `config.json` - файл конфигурации
- `numbers.json` - файл с сохраненными данными
- `region.json` - сохраненная область экрана
- `requirements.txt` - зависимости Python

### 🔧 Скрипты:
- `install.sh` - скрипт автоматической установки (macOS)
- `run.sh` - скрипт для запуска программы
- `demo.py` - создание демонстрационных данных
- `README.md` - этот файл с инструкциями

## ⚙️ Конфигурация

### Параметры в config.json:
- `history_size` - количество отображаемых чисел в истории
- `scan_interval` - интервал сканирования в секундах
- `debug_mode` - режим отладки
- `output_file` - файл для сохранения данных
- `region_file` - файл для сохранения области экрана
- `tesseract_config` - настройки Tesseract OCR
- `image_processing` - параметры обработки изображений:
  - `enhance_contrast` - улучшение контраста
  - `sharpen` - заточка изображения
  - `threshold` - пороговое значение
  - `clahe_clip_limit` - лимит CLAHE
  - `clahe_tile_grid` - размер сетки CLAHE
- `table_display` - настройки отображения таблиц:
  - `use_colors` - использовать цветовое кодирование
  - `table_format` - формат таблиц (grid, simple, fancy_grid)
  - `max_rows` - максимальное количество строк для отображения
  - `show_stats` - показывать статистику
- `thresholds` - пороговые значения для анализа:
  - `warning_time` - время предупреждения
  - `critical_time` - критическое время
  - `max_count_warning` - предупреждение по счетчику
- `stats_file` - файл для сохранения статистики

### Пример конфигурации:
```json
{
  "history_size": 15,
  "scan_interval": 0.5,
  "debug_mode": true,
  "image_processing": {
    "threshold": 100,
    "clahe_clip_limit": 3.0
  },
  "table_display": {
    "use_colors": true,
    "table_format": "grid",
    "max_rows": 20,
    "show_stats": true
  },
  "thresholds": {
    "warning_time": 30.0,
    "critical_time": 50.0,
    "max_count_warning": 5
  }
}
```

## 📊 Формат данных JSON

```json
{
  "last_updated": "2024-01-01T12:00:00",
  "total_count": 10,
  "system_info": {
    "platform": "Darwin arm64",
    "python_version": "3.11.0"
  },
  "numbers": [
    {
      "number": "123.45",
      "timestamp": "2024-01-01T12:00:00",
      "id": 1
    }
  ]
}
``` 